# Components

This folder is reserved for reusable UI or calculation components (e.g. shared SVG templates or future modular scripts). The main app runs from `index.html`, `style.css`, and `script.js` for GitHub Pages compatibility.
